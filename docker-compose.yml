services:
  web:
    image: node:22
    ports:
      - "5173:5173"
      - "9005:9005"
    tty: true
    stdin_open: true
    volumes:
      - .:/app
      - node_modules:/app/node_modules
    working_dir: /app
    command: sh -c "npm install && npm run dev"

volumes:
  node_modules:
